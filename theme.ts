import { DefaultTheme } from "react-native-paper"

export const theme = {
  ...DefaultTheme,
  roundness: 2,
  colors: {
    ...DefaultTheme.colors,
    primary: "#32a852",
    accent: "#015e1a"
  }
}
